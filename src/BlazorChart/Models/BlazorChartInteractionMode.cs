namespace BlazorChart;

/// <summary>Interaction mode used to determine which items are active on hover.</summary>
public enum BlazorChartInteractionMode
{
    Nearest,
    Index,
    Dataset,
    Point,
    X,
    Y
}
